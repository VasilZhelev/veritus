// VIN Check page temporarily removed
// This page has been disabled and will redirect to home

import { redirect } from "next/navigation";

export default function VinCheckPage() {
  redirect("/");
}
